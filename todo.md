  // TODO
        // Rule -> try to not overlap bubbles.
up can slow
down can be faster
when circle inside area of another -> push outside area


some color profiles

demo -> 
z default order power/cad/speed/hr
z prop for wide
z focus/show detail x fields ratio > .8 etc.
z cadance 3 sec show detail, 3 sec hide detail

always calc all fields 
array with 
value / ratio / max / avg
plan:
? track TSS over week..
add vo2max formula -> ?? 

nice html page showing the colors per percentage per scheme


bubbles:

initial best is that the circles (up to 10) starts in their designated position 
initial - ratio is 0, minimal circles on bottom of field
ratio 0 - 100 - circle bigger and floating against top of field
ratio higher - circle bigger 
behaviour
if one circle is biggest - is ok (must stay in field)
if two circles are bigger and competing - both shrink down to make space for smaller circles
in general 
no overlapping circles, circles can be pushed left/down/right to make space for a bigger one
when bigger circle has ratio 0 it eases down to bottom of field and shrinks.

